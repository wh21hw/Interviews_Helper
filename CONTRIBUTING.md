# 贡献指南

感谢参与改进。提交代码前请先创建独立分支，并确保：

1. 不提交 Cookie、浏览器 Profile、代理凭据、账号信息、验证码截图或完整抓取数据。
2. 新增站点适配时遵守目标站点公开规则；不得加入验证码绕过、签名伪造、账号池或高频并发逻辑。
3. 正文选择器、接口字段或 OCR 规则发生变化时，增加一个不含个人信息的最小测试样例。
4. 题目数据必须保留来源 URL 和证据位置；无法核验的内容标为低置信度或不收录。
5. 运行 `python -m unittest discover -s tests -v`，确认全部测试通过。

Bug 报告请包含操作系统、Python 版本、运行命令和脱敏后的错误信息。不要粘贴 Cookie、请求头或浏览器存储内容。

代码采用 MIT License。抓取或整理的数据仍受原作者、来源平台条款及当地法律约束，不因代码开源自动获得再分发许可。
