# 开发路线

以下顺序按数据可信度和开源可维护性排序，不按功能数量排序。

## P0：公开发布前

- [x] 按平台、流水线、存储、检索和报告划分模块；
- [x] 增加 `pyproject.toml`、依赖范围和 `interviews-helper` 命令；
- [ ] 为每个平台建立脱敏 HTML fixture，测试正文边界、登录页和页面改版；
- [x] 增加运行清单：任务参数、成功项、失败项、时间和版本；
- [x] 增加失败记录和按任务指纹断点续跑；
- [ ] 加入面经相关性评分，标记推广、招聘信息、纯教程、评论区和付费截断；
- [ ] 增加人工审核状态，低置信度内容默认不进入高质量题库；
- [ ] 对发布包执行密钥扫描，并验证 Profile、图片、原始正文和真实报告未被 Git 跟踪。

## P1：提高数据质量

- [ ] 规则抽取与可选 LLM 抽取并行，保存两者差异供审核；
- [ ] 支持复合题拆分、题目改写、OCR 纠错和原文证据回链；
- [ ] 在现有“规范化字符串 + 编辑距离”和检索侧向量召回之上加入交叉编码复核；
- [ ] 为公司、部门、岗位、轮次、年份和业务建立独立字段及受控词表；
- [ ] 使用图片内容哈希去重，保存每页 OCR 置信度和失败原因；
- [ ] 对每个平台维护抽样标注集，分别报告题目抽取 Precision/Recall；
- [ ] 增加 CSV、JSONL 和 Markdown 导出，以及只导出高置信度数据的开关。

## P1：提高采集可靠性

- [ ] 使用平台适配器协议支持站内关键词发现，而非依赖人工 URL 文件；
- [ ] 保存搜索游标、已见内容 ID 和最后成功位置，实现真正的增量采集；
- [ ] 按域名配置限速、随机抖动、每日预算和冷却时间；
- [ ] 使用错误类型控制指数退避，不对验证码和登录墙自动重试；
- [ ] 加入页面结构健康检查，选择器失效时停止该平台并生成诊断报告；
- [ ] 支持 Windows、macOS 和 Linux 的 Chrome 路径发现与启动脚本。

## P2：产品体验

- [ ] 为大题库加入全文索引、分页或虚拟列表，避免一次渲染数千张卡片；
- [ ] 增加审核台：查看原文证据、合并/拆分题目、修正标签和屏蔽来源；
- [ ] 显示一次采集任务的覆盖率、失败率、重复率和来源质量分布；
- [ ] 支持保存筛选条件、生成复习清单和按知识点导出面经；
- [ ] 提供本地 WebUI，同时保留零服务依赖的静态 HTML 输出。

## Agent 阶段

- [ ] `curate`：单篇结构化整理，固定 Schema，可选择本地模型；
- [ ] `verify`：逐题检查答案或题面是否被证据支持；
- [ ] `discover`：根据覆盖缺口生成搜索词，只输出候选任务，不直接访问平台；
- [ ] 缓存所有模型调用，提供费用上限、超时和完全关闭 Agent 的选项；
- [ ] 建立人工评测集后再考虑多 Agent，不以主观观感代替指标。
