# 数据使用与发布边界

MIT License 只适用于本项目代码和项目文档，不自动适用于采集到的文章、图片、OCR 文本和面试经验。

- 默认仅采集用户能够正常访问的公开页面。
- 不绕过登录、验证码、付费墙或其他访问控制。
- 不采集评论、手机号、邮箱、学校等与题库无关的个人信息。
- 本地原始数据默认由 `.gitignore` 排除。
- 对外发布题库前，应只保留必要的改写问题、来源链接和统计信息，并检查来源平台规则及作者授权范围。
- 收到合法删除请求后，应能通过 `source_id` 定位并移除来源及其题目关联。

搜索引擎摘要或不完整页面必须标记为 `indexed`、`partial` 或低置信度，不能伪装成完整原文。
