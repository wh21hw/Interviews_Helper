# 安全说明

## 支持范围

项目只处理用户在本机真实浏览器中自行完成的登录。程序不要求复制 Cookie，也不读取、打印或导出浏览器 Cookie 和本地存储。

## 报告问题

请通过私密渠道向维护者报告可能导致登录态、个人数据或本地文件泄露的问题。在修复发布前，不要公开真实凭据、完整 Profile 或可复现的账号攻击材料。

## 本地数据

以下内容默认不进入 Git：浏览器 Profile、OCR 图片、原始快照、SQLite 数据库、合并后的正文、诊断截图和本地配置。发布仓库前仍应执行 `git status --ignored` 复查。
